version: "3.9"

services:
  aegis:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    volumes:
      - ./active_war_room:/app/active_war_room
    restart: unless-stopped
